{
  "name": "microservices",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "skaffold dev --no-prune=false --cache-artifacts=false",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
